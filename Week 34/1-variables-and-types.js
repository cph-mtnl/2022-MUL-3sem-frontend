// Konstant variabler der ikke kan ændre sig
const myName = "Mathias Nielsen"
const myJob = 'Adjunkt'

const myTitle = myName + ", " + myJob
console.log(myTitle)

// .length er en ud af mange string metoder
const titleLength = myTitle.length

// Den næste type er Numbers.
console.log(titleLength)

// Some different numbers
const year = 2022
const month = 8
const date = 22

// Den sidste simple type aka. "Primitive" type: Boolean
// Booleans kan være true/false
const floatNumberOne = 0.1
const floatNumberTwo = 0.2
// Burde være 0.3
const result = floatNumberOne + floatNumberTwo
console.log(result)

// But it is: 0.30000000000000004

// Variabel der kan ændrer sig undervejs
// let 

// Var er den gamle udgave af let
// var